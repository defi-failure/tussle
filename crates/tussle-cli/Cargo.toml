[package]
name = "tussle-cli"
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "tussle"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
